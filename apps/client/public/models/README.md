Place the `face-api.js` model files for these networks in this folder before running the employee monitor:

- `tiny_face_detector_model-*`
- `face_landmark_68_model-*`
- `face_expression_model-*`

The app loads them from `/models` via `VITE_FACE_MODEL_URL`.
